<div class="comment-container">
    {{#each this}}
    <section class="comment">
        <div class="comment-text">{{comment_text}}</div>
        <div class="metadata">-{{user.username}}, {{format_date created_at}}</div>
    </section>
    {{/each}}
</div>