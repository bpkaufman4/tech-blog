<article>
    <div>
        <h1>{{title}}</h1>
    </div>
    <div>
        <p>{{content}}</p>
    </div>
    <div>
        <p>created by {{user.username}} on {{created_at}}</p>
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
    </div>
</article>